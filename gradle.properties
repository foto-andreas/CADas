org.gradle.caching=true
# Configuration Cache ist für das macOS-Bundling (jpackage/jlink via Exec-Tasks) deaktiviert,
# da die Paketierungsaufgaben system- und prozessabhängig arbeiten und nicht CC-kompatibel sind.
org.gradle.configuration-cache=false
org.gradle.parallel=true

